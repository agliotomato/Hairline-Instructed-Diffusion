# V4 아키텍처: 헤어라인 컨디셔닝 및 특징 추출

## 1. 문제 정의: 불충분한 컨디셔닝 (Insufficient Conditioning)
**진단:** 현재의 단일 채널 공간 맵(Single-channel Spatial Map, 바이너리 마스크)을 UNet에 직접 입력하는 방식은 근본적인 결함이 있다.

### 단일 채널 마스크로 인한 불충분한 컨디셔닝 사례 예시 
<div align="center">
  <img src="results/v3_innov_test/sample_20251206_101250_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101550_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101621_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101650_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101720_000.png" width="18%" />
</div>
<br>

| 현상 | 설명 |
| :--- | :--- |
| **정보 붕괴 (Information Collapse)** | 단순 다운샘플링된 마스크는 고주파(High-frequency) 세부 정보를 잃는다. "어디에(Where)"는 전달하지만 "무엇을(What, 예: 뾰족함)"은 전달하지 못한다. |
| **허위 상관관계 (Spurious Correlation)** | 명시적인 힌트가 없으면 모델은 잘못된 인과관계를 환각(Hallucinate)할 위험이 있다 (예: "눈매가 날카로우니 머리도 뾰족할 것이다"). 이는 **진정한 제어(Genuine Control)**가 아니다. |
| **의미적 간극 (Semantic Gap)** | UNet은 해당 영역을 *왜* 특정 방식(방향, 곡률 등)으로 복원해야 하는지에 대한 신호를 받지 못한다. |

> **핵심 통찰:** "UNet에게 '알아서 해석해'라고 맡기지 말고, **명시적 특징 추출(Explicit Feature Extraction)**을 통해 구체적인 힌트를 줘야 한다."

## 2. 제안된 방법론: 간극 해소
이 문제를 해결하기 위해 저차원 공간 마스크를 고차원 의미 임베딩(High-dimensional Semantic Embedding)으로 끌어올려야 한다.

### A. 학습 가능한 컨볼루션 인코더 (채택됨)
![Convolutional Encoder Diagram](assets/conv_encoder.png)

이 방식은 **ControlNet** 아키텍처와 유사하다.
*   **메커니즘**: 입력(1ch) $\rightarrow$ Conv 레이어 $\rightarrow$ 다채널 특징 맵(예: 64ch).
*   **효과**: 형태학적 정보(가장자리, 날카로움)를 잠재 특징 맵(Latent Feature Map)으로 인코딩한다.
*   **통합**: 의미적 맥락을 가지고 노이즈 제거(Denoising) 과정을 가이드하기 위해 UNet(보통 덧셈 방식)에 주입된다.

### B. VAE 인코더 (Latent Encoding)
*   **메커니즘**: 마스크를 사전 학습된 VAE 인코더에 통과시켜 메인 모델의 잠재 공간(Latent Space)과 일치하는 표현을 얻는다.
*   **장단점**: 풍부한 의미를 담을 수 있지만, 구현 복잡도와 연산 비용이 높다.

## 2.1. 특징 주입 전략: 왜 Concat이 아닌 Add인가?
기존 V3 방식은 입력단에서 채널을 물리적으로 붙이는 (Concat) 방식을 사용했으나, V4에서는 **중간층 덧셈 주입 (Additive Injection)** 방식을 채택한다.

### 기존 방식: Input Concatenation
*   **구조:** `Cat([Latent(4ch), Mask(1ch)])` $\rightarrow$ `ConvLayer(5ch input)`
*   **문제점:**
    *   **차원 불일치:** 인코더를 거친 고차원 특징(64ch)을 붙이면 입력 채널이 비대해짐 (4+64=68ch).
    *   **가중치 파괴:** UNet의 첫 번째 레이어 구조를 변경해야 하므로, 사전 학습된(Pre-trained) 가중치를 활용하기 어려움 (Fine-tuning 효율 저하).

### V4 방식: Additive Injection with Zero Convolution
![Additive Injection Diagram](assets/zero_conv_injection.png)

ControlNet이나 Adapter에서 사용하는 정석적인 방법이다.

*   **구조:** `UNet_Feature + ZeroConv(Encoder_Feature)`
*   **Zero Convolution의 역할:**
    *   초기 가중치와 편향(Bias)이 `0`으로 설정된 컨볼루션 레이어.
    *   학습 초기에는 주입되는 신호가 `0`이므로, **기존 UNet의 성능과 지식을 100% 보존**한 상태로 시작한다.
    *   학습이 진행됨에 따라 점진적으로 유의미한 특징을 주입한다.

> **결론:** "UNet의 지식을 해치지 않으면서(Feature Preservation), 우리가 원하는 명시적인 특징만 살짝 얹어주는(Feature Injection) 가장 안전하고 효과적인 방법이다."

## 3. 상세 구현 명세 (Technical Specification)

**Pixel-space ControlNet** 방식을 채택한다. 이는 사용자 피드백(Aliasing 문제)과 ControlNet 원논문의 설계를 반영한 결정이다.

### 3.1. 아키텍처 토폴로지 (Topology): Pixel-space ControlNet
*   **Main Backbone**: `Stable Diffusion v1.5 UNet` (Frozen).
*   **Conditioning Module**: `Standard ControlNet` (Trainable).
    *   **입력**: $512 \times 512$ (또는 $256 \times 256$) **고해상도 마스크**.
    *   **Encoder**: **Tiny Encoder** (4-layer CNN with strides)가 이를 $64 \times 64$ Feature Map으로 점진적 압축.
    *   **Injection**: 압축된 Feature를 UNet에 Additive Injection.

### 3.2. Why Pixel-space? (Rationale)
1.  **Aliasing 방지**: $512 \times 512$ 마스크를 $64 \times 64$로 단순 다운샘플링(Nearest Neighbor)하면, 얇은 헤어라인(High-frequency)이 계단 현상으로 뭉개지거나 소실된다.
2.  **학습 가능한 다운샘플링 (Learnable Downsampling)**: Tiny Encoder는 단순한 축소가 아니라, "어떤 디테일을 보존해야 복원에 유리한지"를 학습한다. 얇은 선이라도 Feature Space에서는 강한 신호로 살아남을 수 있다.
3.  **Reference**: ControlNet 논문에서도 Canny Edge 등을 처리할 때 $512 \times 512$ 입력을 받아 내부 Encoder로 처리한다.

### 3.3. Training Pipeline Flow
1.  **Input Data**:
    *   `Noisy Latent` $z_t$ ($4 \times 64 \times 64$)
    *   `Hairline Mask` $m$ ($1 \times 512 \times 512$) - **No Downsampling**
2.  **ControlNet Forward**:
    *   `Conditioning Embedding`: $m (512 \times 512) \xrightarrow{Conv \times 4} F_{cond} (64 \times 64)$
    *   `Main Process`: $F_{cond}$가 Zero Convolution을 거쳐 UNet Feature에 더해짐.
3.  **Optimization**:
    *   오직 ControlNet 파라미터(Encoder 포함)만 업데이트.

### 3.4. 1-Channel Adaptation
*   Standard ControlNet의 입력 채널을 3(RGB)에서 **1(Gray/Mask)**로 변경하여 초기화.
*   `controlnet_cond_embedding.conv_in`: `Conv2d(1, 16, ...)`으로 수정 및 Random Init.

### 3.5. 자주 묻는 질문 (FAQ)
*   **Q: ControlNet을 2개 쓰는 건가요?**
    *   **A: 아닙니다. 단 1개만 사용합니다.**
    *   기존 V3의 IdentityNet을 **제거**하고, V4 Standard ControlNet **단 1개**로 대체하는 것입니다.

*   **Q: 대머리(Bald) 이미지는 어떻게 쓰나요?**
    *   **학습 (Training)**: **사용하지 않습니다 (Excluded).**
        *   이유: 대머리 이미지를 Conditioning으로 주면, 모델이 마스크(형태)를 보지 않고 대머리 이미지의 피부 경계를 보고 헤어라인을 추측하는 **Cheating(컨닝)** 현상이 발생합니다. 이를 원천 차단하기 위해 학습 입력에서 완전히 배제합니다.
    *   **추론 (Inference)**: **초기 Latent (Base Canvas)**로 사용됩니다.
        *   생성 과정에서 얼굴 아이덴티티를 유지하기 위한 시작점(Initial Noise Base)으로만 활용됩니다.
