# Project V4: Dual-Stream Adapter for High-Fidelity Hair Generation

## 1. 개요 (Executive Summary)
본 제안서는 기존 V3 모델의 한계점인 **'고주파 정보 소실(Information Collapse)'**과 **'허위 상관관계(Spurious Correlation) 학습'** 문제를 해결하기 위해 설계된 V4 아키텍처를 기술한다.

핵심 변경 사항은 단일 채널 마스크 입력을 **학습 가능한 'Tiny Encoder'**를 통해 다채널 특징(Multi-channel Feature)으로 변환하는 것과, 형태(Geometry)와 신원(Identity) 정보를 분리하여 주입하는 **'Dual-Stream Adapter'** 구조의 도입이다.

---

## 2. 문제 정의 (Problem Statement)
지난 연구(V3) 및 피드백을 통해 식별된 핵심 문제는 다음과 같다.

### 2.1. 불충분한 컨디셔닝 (Insufficient Conditioning)
*   **현상**: $512 \times 512$ 해상도의 얇고 뾰족한 헤어라인(High-frequency details)을 $64 \times 64$로 단순 다운샘플링(Nearest/Bilinear)할 경우, 공간적 정보가 뭉개져(Blurry) 전달됨.
*   **결과**: UNet은 구체적인 형태 지시를 받지 못하고, "대략 이 위치에 무언가 있다" 정도만 인지하여 디테일 복원에 실패함.

#### 사례: 단일 채널 마스크로 인한 정보 붕괴
<div align="center">
  <img src="results/v3_innov_test/sample_20251206_101250_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101550_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101621_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101650_000.png" width="18%" />
  <img src="results/v3_innov_test/sample_20251206_101720_000.png" width="18%" />
</div>

| 현상 | 설명 |
| :--- | :--- |
| **정보 붕괴 (Information Collapse)** | 단순 다운샘플링은 "어디에"는 전달하지만 "무엇을(예: 뾰족함)"은 전달하지 못함. |
| **의미적 간극 (Semantic Gap)** | UNet은 해당 영역을 *왜* 특정 방식(방향, 곡률)으로 복원해야 하는지 모름. |

### 2.2. 허위 상관관계 및 컨닝 (Spurious Correlation & Leakage)
*   **현상**: 대머리 이미지를 그대로 조건으로 사용할 경우, 모델이 헤어 마스크(형태)를 따르지 않고 대머리 이미지의 피부 경계(Texture Boundary)를 보고 헤어라인을 단순 추론(Cheating)함.
*   **결과**: 사용자가 원하는 새로운 헤어 스타일로의 제어(Control)가 불가능해짐.

---

## 3. 제안된 아키텍처: Hybrid Dual-Stream Adapter (V4)
우리는 **Pixel-space**의 고해상도 정보와 **Latent-space**의 효율적인 신원 정보를 결합하는 **Hybrid Architecture**를 제안한다.

### 3.1. 아키텍처 다이어그램 (Topology)

![Architecture Diagram](assets/diagram.png)

### 3.2. 핵심 모듈 상세 (Component Details)

#### A. Stream 1: Geometry Guide (Pixel-Space)
*   **입력**: $512 \times 512$ 고해상도 **바이너리 마스크**.
*   **모델**: Standard `ControlNetModel` (Tiny Encoder 포함).
*   **특징**: `Stride=2` 다운샘플링 레이어를 통해 고해상도 마스크의 미세한 곡률과 뾰족한 디테일(Geometry)을 손실 없이 압축하여 전달한다.
*   **Arch Ref**: ![Tiny Encoder](assets/conv_encoder.png)
*   **목적**: "어떤 모양의 머리카락을 만들 것인가?" (Shape Control)

#### B. Stream 2: Identity Context (Latent-Space)
*   **입력**: $64 \times 64$ **Masked Bald Latent** (VAE Encoded).
*   **모델**: `latent_identity_net.py` (V3 Legacy).
*   **특징**: `Stride=1` 구조로, 이미 압축된 Latent 정보를 받아 추가적인 압축 없이 신원 정보(Identity)를 추출한다.
*   **목적**: "누구의 얼굴에 머리카락을 붙일 것인가?" (Identity Preservation)

#### C. Hybrid Injection Mechanism
*   **방식**: 두 스트림의 출력을 **덧셈(Add)**으로 결합한다.
*   **이점**: 
    1.  **형태 보존**: 512px 마스크에서 추출한 선명한 엣지 정보가 유지됨.
    2.  **신원 유지**: 검증된 V3 방식의 Latent Identity Net을 사용하여 얼굴 왜곡을 방지함.
*   **Ref**: ![Zero Conv](assets/zero_conv_injection.png)

---

## 4. 데이터 파이프라인 및 학습 전략 (Training Strategy)

### 4.1. 데이터셋 구성 (Data Preparation)
학습 시 모델이 "형태"와 "아이덴티티"를 혼동하지 않도록 엄격한 전처리를 수행한다.

| 입력 변수 | 데이터 소스 | 전처리 방식 (Preprocessing) |
| :--- | :--- | :--- |
| **Ground Truth** | 원본 이미지 | $512 \times 512$ Resize, Normalize |
| **Stream A (Geo)** | 헤어 마스크 | Face Parsing Map에서 Hair 영역 추출 (Binary, 1ch) |
| **Stream B (ID)** | Masked Bald | 원본 $\times$ $(1 - \text{Mask})$ $\rightarrow$ **VAE Encode** ($64 \times 64$ Latent) |

### 4.2. 학습 로직 (Optimization)

#### # 학습 (Training Details)
학습 과정에서 **어떤 부분이 유지되고, 어떤 부분이 새로 학습되는지**는 다음과 같다.

| 구분 | 컴포넌트 (Component) | 상태 (Status) | 설명 |
| :--- | :--- | :--- | :--- |
| **유지 (Preserved)** | **Stable Diffusion UNet** | **Frozen ❄️** | SD1.5의 사전 학습된 생성 능력(Prior)을 그대로 보존함. |
| | **VAE (Autoencoder)** | **Frozen ❄️** | 이미지 $\leftrightarrow$ Latent 변환 담당. 가중치 고정. |
| | **CLIP Text Encoder** | **Frozen ❄️** | 텍스트 프롬프트 이해 담당. 가중치 고정. |
| **학습 (Newly Learned)** | **Stream A (Tiny Encoder)** | **Trainable 🔥** | 512px 마스크를 해석하는 능력을 **처음부터(혹은 초기화 후) 학습**. |
| | **Stream B (Identity Net)** | **Trainable 🔥** | 얼굴 정보를 추출하여 UNet에 주입하는 방법을 학습. |
| | **Zero Convs** | **Trainable 🔥** | 두 스트림의 정보를 UNet에 얼마나/어떻게 더할지($\alpha$)를 학습. |

*   **Objective**: $L = \mathbb{E}_{z, t, c_A, c_B, \epsilon} [\|\epsilon - \epsilon_\theta(z_t, t, c_A, c_B)\|]$
*   **특이 사항**: 대머리 이미지를 그대로 넣지 않고, 반드시 **마스킹된 이미지**를 넣어 Identity Net이 형태 정보(Shape)를 배우지 못하게 강제함.

---

## 5. 예상되는 개선 효과 (Expected Improvements)

| 구분 | V3 (Previous) | V4 Hybrid (Proposed) |
| :--- | :--- | :--- |
| **입력 해상도** | 64px (Latent All) | **512px (Geo) + 64px (ID)** |
| **형태 표현력** | 낮음 (Aliasing 발생) | **매우 높음 (Pixel-perfect Control)** |
| **신원 안정성** | 불안정 | **안정적 (Legacy Latent Net 활용)** |
| **구조적 유연성** | 단일 스트림 | **Dual-Stream (독립적 제어 가능)** |

---


## 6. 학습

```bash
python3 train_hairline_cond_v4.py \
  --pretrained_model_name_or_path "runwayml/stable-diffusion-v1-5" \
  --output_dir "hairline_cond_v4_hybrid" \
  --orig_dir "data/original_images" \
  --bald_dir "data/bald_images" \
  --mask_dir "data/only_forehead_line" \
  --resolution 512 \
  --learning_rate 1e-5 \
  --train_batch_size 4 \
  --gradient_accumulation_steps 1 \
  --mixed_precision "fp16" \
  --num_train_epochs 200 \
  --checkpointing_steps 500 \
  --checkpoints_total_limit 3 \
```

## 결론 (Conclusion)
V4 Hybrid 아키텍처는 **"Pixel-space의 정밀함"**과 **"Latent-space의 안정성"**을 결합한 최적의 솔루션입니다. Tiny Encoder는 머리카락 한 올의 디테일을 살리고, Latent Identity Net은 기존에 검증된 방식대로 모델의 신원을 잡아줍니다. 이 두 가지를 결합함으로써 V3의 한계를 완전히 극복할 수 있습니다.
