# SONIC Hybrid Architecture Strategy
**(Spectral Optimization of Noise for Inpainting with Consistency)**

이 문서는 **SONIC 논문**의 핵심 기술을 현재 진행 중인 **SD3.5 Hybrid Inpainting** 프로젝트에 적용하기 위한 전략을 기술합니다.

## 1. 배경: 왜 랜덤 노이즈가 문제인가? (Why Random Noise fails?)
기존의 인페인팅 방식(Blended Latent Diffusion 등)은 마스크 영역을 단순한 랜덤 가우시안 노이즈로 채웁니다. 하지만 이는 **"초기 시드 노이즈 자체가 최종 이미지의 구조(Layout)와 사물의 배치를 이미 함축하고 있다"**는 사실을 간과한 것입니다.

*   **구조적 불일치 (Structural Mismatch)**: 랜덤 노이즈가 가진 잠재적 구조가 원본 배경의 구조와 어긋날 경우, 아무리 강력한 모델이라도 경계선이 어색하거나 조명이 튀는(Artifact) 결과물을 만듭니다.
*   **해결책**: 마스크 영역 안의 노이즈를 **"마스크 밖의 배경을 생성했을 법한 노이즈"**로 역산하여 수정하는 과정이 필수적입니다.

## 2. 작동 원리: 20~40회의 노이즈 최적화 (Noise Optimization Loop)
최적화 단계는 모델 파라미터를 학습시키는 것이 아니라, **입력되는 노이즈 값($z_T$) 자체를 업데이트**하는 과정입니다.

### 2.1. 선형 근사 (Linearized Denoising)
SD3.5와 같은 거대 모델에서 역전파(Back-propagation)를 통해 노이즈를 최적화하는 것은 메모리 비용이 엄청납니다. SONIC은 이를 해결하기 위해 **디노이징 경로를 하나의 직선**으로 가정합니다.
*   현재 노이즈 $x_T$에서 예측된 결과물 $\mathcal{D}_T(x_T)$까지의 경로를 고정된 상수로 취급하여 계산량을 획기적으로 줄입니다. 

### 2.2. 손실 함수 (Masked MSE Loss)
최적화의 목표는 명확합니다. "이 노이즈($x_T$)로 그림을 그렸을 때($\mathcal{D}_T$), **마스크 밖의 영역($1-A$)**이 원본 이미지($y$)와 똑같아야 한다"는 것입니다.

$$ Loss = || y - (1-A) \cdot \mathcal{D}_T(x_T) ||^2 $$

이 손실 값을 바탕으로 오직 **초기 노이즈 $z_T$**의 값만 미세하게 조정합니다.

## 3. 핵심 디테일: 스펙트럼 영역 최적화 (Spectral Domain Optimization)
단순히 픽셀(Spatial) 값을 조정하면 노이즈가 깨지거나 과포화되기 쉽습니다. SONIC은 노이즈를 **푸리에 변환(FFT)**하여 주파수 영역에서 최적화합니다.

*   **안정적 수렴**: 저주파(전체적 형태/볼륨)와 고주파(세부 질감)의 수렴 속도를 맞춰주어, 노이즈가 가우시안 분포를 벗어나 오염되는 것을 막습니다.
*   **공명(Resonance)**: 최적화가 진행될수록 마스크 영역 안의 노이즈는 배경의 질감, 조명, 구도와 구조적으로 **'공명'**하는 상태가 됩니다.

